<UserControl x:Class="OficinaVirtual.Views.CanvasTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="6" Background="#FFF0F0F0">
            <Button x:Name="RefreshButton" Content="Actualizar lista" Padding="6,3" Margin="0,0,6,0" Click="RefreshButton_Click"/>
            <ComboBox x:Name="WindowsCombo" Width="300" Margin="0,0,6,0"/>
            <Button x:Name="AddButton" Content="+ Pinchar" Padding="6,3" Margin="0,0,6,0" Click="AddButton_Click"/>
            <Button x:Name="ReleaseButton" Content="- Liberar" Padding="6,3" Margin="0,0,16,0" Click="ReleaseButton_Click"/>
            <TextBlock x:Name="ZoomText" Text="100%" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,16,0"/>
            <TextBlock Text="rueda = zoom · arrastra el fondo = mover el canvas · clic = seleccionar, clic de nuevo = abrir"
                       VerticalAlignment="Center" Foreground="Gray" FontSize="11"/>
        </StackPanel>

        <Border Grid.Row="1" ClipToBounds="True" Background="#FF0A0C12" x:Name="ViewportBorder"
                MouseWheel="Viewport_MouseWheel"
                MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                MouseMove="Viewport_MouseMove">
            <Canvas x:Name="InfiniteCanvas" Width="4000" Height="4000">
                <Canvas.Background>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,50,50" ViewportUnits="Absolute" Stretch="None">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing>
                                <GeometryDrawing.Pen>
                                    <Pen Brush="#FF1E2433" Thickness="1"/>
                                </GeometryDrawing.Pen>
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <LineGeometry StartPoint="0,0" EndPoint="50,0"/>
                                        <LineGeometry StartPoint="0,0" EndPoint="0,50"/>
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Canvas.Background>
                <Canvas.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="CanvasScale"/>
                        <TranslateTransform x:Name="CanvasTranslate"/>
                    </TransformGroup>
                </Canvas.RenderTransform>
            </Canvas>
        </Border>
    </Grid>
</UserControl>
